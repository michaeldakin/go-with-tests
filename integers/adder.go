package integers

// Adder() calls Add() and returns its value
// Example: Adder(7, 13)
// Returns: 20
func Adder(x, y int) int {
	return Add(x, y)
}

// Takes two integers adn returns their sum
func Add(a, b int) int {
	return a + b
}
